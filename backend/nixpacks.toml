[phases.setup]
nixPkgs = ['python311', 'gcc', 'pkg-config', 'openssl']

[phases.install]
cmds = ['pip install --upgrade pip setuptools wheel', 'pip install --no-cache-dir -r requirements.txt']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'